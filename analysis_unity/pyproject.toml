[project]
name = "adui-analysis-unity"
version = "0.1.0"
description = "Unity telemetry analysis pipeline for ADUI."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
  "matplotlib>=3.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

