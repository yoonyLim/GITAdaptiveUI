[project]
name = "adui-analysis-multigame"
version = "0.1.0"
description = "Public multi-game situation-recognition pretraining for ADUI Bayesian priors"
requires-python = ">=3.10"
dependencies = ["pyarrow", "matplotlib", "vizdoom"]
